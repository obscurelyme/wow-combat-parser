export async function loader() {
  // const contact = await getContact(params.contactId);
  // if (!contact) {
  //   throw new Response('', {
  //     status: 404,
  //     statusText: 'Not Found',
  //   });
  // }
  // return { contact };
  return null;
}
